---
- name: Deploy to K8s Cluster
  hosts: localhost
  connection: local
  become: true
  tasks:
  - name: Deploy Pod
    shell: |
      kubectl apply -f deployment.yml

  - name: Deploy Service
    shell: |
      kubectl apply -f service.yml
