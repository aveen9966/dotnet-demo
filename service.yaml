apiVersion: v1
kind: Service
metadata:  
  name: dotnet-demo-service
spec:
  selector:    
    app: dotnet-app
  type: NodePort
  ports:  
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30036
    protocol: TCP
